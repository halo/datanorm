#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'datanorm'

ENV['DEBUG_DATANORM'] = 'true'

path = ARGV.first
document1 = Datanorm::Document.new(path:)

document1.items do |item|
  puts item
  break if item.index > 100
end
